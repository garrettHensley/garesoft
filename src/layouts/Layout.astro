

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>My Dev Blog</title>
    <div class="container">
      <div>
        <a href="/" id="home-link">
          garesoft
        </a>
      </div>
      <article>
        <slot />
      </article>
    </div>
    <style>
      :root {
        --bg-light: #F5ECE0;
        --primary: #5F99AE;
        --secondary: #336D82;
        --accent: #693382;
        --text-dark: #1E1E1E;
        --text-light: #ffffff;
      }

      html body {
        background-color: var(--bg-light);
        color: var(--text-dark);
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 0;
        height: 100vh;
      }

      #home-link {
        color: black;
        text-decoration: none !important;
      }

      header {
        background: var(--primary);
        color: var(--text-light);
        padding: 1rem 2rem;
        display: flex;
        justify-content: space-between;
        align-items: center;
      }

      nav a {
        color: var(--text-light);
        text-decoration: none;
        margin-left: 1rem;
        font-weight: bold;
      }

      .hero {
        text-align: center;
        padding: 3rem 1rem;
        background: var(--secondary);
        color: var(--text-light);
      }

      .container {
        max-width: 800px;
        margin: 2rem auto;
        padding: 3rem;
        box-shadow: 3px 3px 3px 0px rgb(130, 124, 124);
      }

      .post {
        background: var(--text-light);
        padding: 1rem;
        border-left: 5px solid var(--accent);
        margin-bottom: 1rem;
      }
    </style>
  </head>
</html>

