

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta property="og:title" content='garesoft.dev' />
    <meta property="og:description" content='Garesoft - Indie Dev Blog' />
    <title>garesoft.dev</title>
    <link rel="icon" href="/public/favicon.png" />
    <div class="container">
      <div id="header-container">
        <a href="/" id="home-link">
          garesoft
        </a>
      </div>
      <main>
        <slot />
      </main>
    </div>
    <style>
      :root {
        --bg-light: #F5ECE0;
        --primary: #494d7e;
        --secondary: #8b6d9c;
        --accent: #f2d3ab;
        --text-dark: #272744;
        --text-light: #fbf5ef;
      }

      html body {
        background-image: url('/images/cobbletone.png');
        background-repeat: repeat;
        background-size: 9%;
        color: var(--text-light);
        margin: 0;
        padding: 0;
        height: 100vh;
        font-family: 'Times New Roman', Times, serif
      }

      #home-link {
        color: var(--text-light);
        text-decoration: none !important;
        font-size: 2rem;
        padding-bottom: 2rem;
      }

      #header-container {
        margin-bottom: .5rem;
      }


      main {
        background-color: var(--text-dark);
      }

      nav a {
        color: var(--text-light);
        text-decoration: none;
        margin-left: 1rem;
        font-weight: bold;
      }

      .hero {
        text-align: center;
        padding: 3rem 1rem;
        background: var(--secondary);
        color: var(--text-light);
      }

      .container {
        max-width: 800px;
        margin: 2rem auto;
      }

      main {
        padding: 3rem;
        border: 17px ridge var(--accent);
      }

      .post {
        background: var(--text-light);
        padding: 1rem;
        border-left: 5px solid var(--accent);
        margin-bottom: 1rem;
      }
    </style>
  </head>
</html>

